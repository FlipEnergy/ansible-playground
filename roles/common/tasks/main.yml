- name: Install tools
  ansible.builtin.apt:
    pkg:
      - bat
      - curl
      - exa
      - hstr
      - iputils-ping
      - neofetch
      - netcat
      - ruby-dev
      - screen
      - unzip
      - vim
    update_cache: yes
  become: yes

- import_tasks: haste.yml
  become: yes
